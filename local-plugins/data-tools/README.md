# data-tools

Data analysis toolkit. Currently one skill:

| Skill | Purpose |
|---|---|
| `duckdb-data` | In-process SQL analytics on CSV/Parquet/JSON — EDA, transforms, window functions, cohort/funnel patterns |

Complements the official Cowork `data` plugin (which only exists in Desktop/Cowork sessions) — this one also works in CLI sessions on Mac and VPS.
